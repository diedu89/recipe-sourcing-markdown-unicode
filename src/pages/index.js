import React from "react"
import { Link } from "gatsby"

const IndexPage = () => (
  <main>
    Sourcing markdown recipe (see page at{` `}
    <Link to="æøå-test">/æøå-test</Link>)
  </main>
)

export default IndexPage
